Run the project state sync routine.

The full prompt, hard rules, file allowlist, edit procedure, compressed
handoff entry template, and archiving logic are defined in
**`docs/templates/sync-state-agent.md`**.

Read that file first and execute it exactly. Do not improvise outside its
hard rules. The same template is used by Codex, so consistency matters.
